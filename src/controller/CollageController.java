package controller;

/**
 * Controller interface for the collager. Allows the starting
 * of the controller so that action can be received by the user,
 * and to allow the corresponding changes to be made to the project
 */
public interface CollageController {

  /**
   * Method to start the controller.
   */
  public void start();
}
